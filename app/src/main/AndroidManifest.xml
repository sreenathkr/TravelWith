<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.reshma.travelwithme">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".registerMobile" />
        <activity android:name=".otpmobile" />
        <activity android:name=".verifyotpScreen" />
        <activity android:name=".signInPage" />
        <activity android:name=".forgotPassword" />
        <activity android:name=".forgotPasswordEmail" />
        <activity android:name=".emailSent" />
        <activity android:name=".verifyEmailScreen" />
        <activity android:name=".registrationPage" />
        <activity android:name=".travelLocal"></activity>
    </application>

</manifest>